name: Playwright Tests

# ✅ Only runs manually from the GitHub Actions tab
on:
  workflow_dispatch:

jobs:
  test:
    if: false # 🚫 disables the workflow entirely for now
    runs-on: ubuntu-latest
    steps:
      - run: echo "Playwright tests are temporarily disabled."
